#AMS Offline Libs
```bash
amsenv              # AFS Offline && unset AMSLIB
cvs co AMS          # AMS02Offline
export AMSWD        # AMS02Offline/AMSWD
export AMSSRC       # AMS02Offline/AMSSRC
cd install          # AMS/install
make -j8 gbatch lib # using 8-process
cvs update          # AMS/ updata Offline
```
